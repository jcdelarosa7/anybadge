name: docker-build
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        working-directory: /home/runner/work/
        run: |
             docker build ../../. --file Dockerfile --tag myapp
